# @package _global_

defaults:
  - override /data: generic_visda2017.yaml
  - override /model: vit.yaml
  - override /trainer: default.yaml

seed: 42

model:
  model_name: "google/vit-base-patch16-224-in21k"

logger:
  wandb:
    id: gyi9o7z0
    project: STL-visda2017
    # name: ${hydra:runtime.choices.experiment}
    name: STL-visda2017/vitb16_che0-ft_augmix
    # tags: ${tags}

ckpt_path: out/synthnet-transfer-learning-outputs/train/multiruns/STL-visda2017/vitb16_che0-ft_augmix/2023-04-25_20-48-52/0/checkpoints/epoch_008.ckpt
