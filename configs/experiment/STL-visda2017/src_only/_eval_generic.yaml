# @package _global_

defaults:
  - override /data: generic_visda2017.yaml
  - override /model: vit.yaml
  - override /trainer: default.yaml

seed: 42

model:
  model_name: "google/vit-base-patch16-224-in21k"

logger:
  wandb:
    # id: c4v21fyt
    project: STL-VisDa2017-Eval
    # name: ${hydra:runtime.choices.experiment}
    name: vitb16_ch_sgd1e-3_e0
    tags: ${tags}

ckpt_path: out/checkpoints/visda2017/vitb16_ch_sgd1e-3_e0.ckpt
