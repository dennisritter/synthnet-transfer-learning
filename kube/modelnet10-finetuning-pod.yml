apiVersion: v1
kind: Pod
metadata:
  name: modelnet10-finetuning-expert-sweep-15
spec:
  restartPolicy: Never
  containers:
  - name: modelnet10-finetuning-expert-sweep-15
    image: beuthdritter/synthnet-finetuning
    env:
    - name: WANDB_API_KEY
      valueFrom:
        secretKeyRef:
          name: wandb-secret
          key: secret
          optional: false
    volumeMounts:
            - name: synthnet-finetuning-volume
              mountPath: /workspace/data
            - name: output-volume
              mountPath: /workspace/out
    command:
      [
        "bash",
        "scripts/modelnet10_vit_finetuning.sh" ,
      ]
    resources:
      limits:
        nvidia.com/gpu: 1
  nodeSelector:
    gpu: v100
  volumes:
  - name: synthnet-finetuning-volume 
    persistentVolumeClaim:
      claimName: synthnet-finetuning-pvc
  - name: output-volume 
    persistentVolumeClaim:
      claimName: output-pvc
